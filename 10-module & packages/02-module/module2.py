# we are going to use the even_num function that we created here
# to do so we have to first import the file
# then do as follows:

import module1
x = input('Enter a number:')
module1.even_num(int(x))

# if we print this we are going to get the results
